# UI 視覺改版規格書（給前端工程師）

> 版本：2026-07-05／發包範圍：**非戰鬥 DOM 介面全部頁面的視覺層**
> 戰鬥畫面（PixiJS canvas）不在此次範圍，但戰鬥資訊 HUD（DOM overlay）包含在內。

---

## 1. 我想要的感覺（Design Direction）

**一句話**：《劍與遠征》(AFK Arena) 那種**溫暖、精緻、有手工質感的中古奇幻**——羊皮紙告示、木刻框、黃銅鉚釘、金綢帶，但畫面乾淨明快、不髒不灰。

目前版本的問題（要修正的方向）：

1. **顏色層次太單一（整體土黃）**——現在整個畫面從底到面板到按鈕都是相近的黃棕色，糊成一片。正確做法是**拉開三個層次的對比**：
   - **底層（最暗）**：深胡桃木／深皮革，接近黑的暖棕（`#2a1c10` 附近），可以帶木紋紋理
   - **內容層（最亮）**：象牙色羊皮紙（偏白的 `#f2e4c2` 而非土黃 `#d6ba8a`），深褐墨字，內容列與底層之間要有明顯明度差
   - **點綴層（彩度）**：金色只給主行動與獎勵；另外引入**寶石色點綴**（藍寶石 CTA、翡翠綠成功、緋紅警示）避免滿版暖色的單調
2. **質感不足**——現在是純 CSS 漸層，希望升級成**貼圖質感**：木紋、紙纖維、金屬浮雕邊框（見 §6 素材清單，可用 9-slice）
3. **裝飾語彙**——AFK 風格的特徵元素：撕邊羊皮紙、木刻橫幅（標題）、六角切角綢帶鈕、黃銅角釘、盾形紋章框。用在正確的位置，不要每個元素都堆滿。

參考基準：AFK Arena 的公會頁／推薦公會頁（用戶提供過截圖，向 PM 索取）。

---

## 2. 技術架構與合約（你能動什麼、不能動什麼）

- 前端是 **Vite + 原生 JS + 純 CSS**，無框架。UI 由 JS 以固定 class 名產生 DOM。
- **合約：JS 不動、class 名不動**——你只改三個 CSS 檔（`src/style.css`、`src/redesign.css`、`src/social.css`）與新增素材檔（`public/assets/ui/`）。若必須加 wrapper 或改 DOM 結構，開需求單回來討論。
- 版面配置（layout）**維持現狀**：各頁的欄位位置、格線、資訊架構都已定案，這次只做視覺層（配色、質感、邊框、字體層級、細節動效）。
- 螢幕：**橫向**手遊比例（16:9 為主），`html { font-size: clamp(9px, 2.05vmin, 16px) }` 全域縮放，尺寸用 `rem`。
- 一切互動元素已有 `.pressable`（按壓縮放回饋），保留。

---

## 3. Design Tokens API（CSS 變數＝合約）

全部定義在 `src/style.css` 的 `:root`。**你可以改變數的值，不要改變數名**（JS 與三個 CSS 檔大量引用）。以下是變數清單與我期望的方向：

```css
:root {
  /* ── chrome：底與面板（要暗、要有質感，拉開與紙的對比） ── */
  --bg:          /* 主底色：深胡桃木，建議 #332413~#3a2a16 */
  --bg-deep:     /* 更深的底（頁面漸層終點/body）：#241708 附近 */
  --panel:       /* 皮革面板：#4a3520 附近 */
  --panel-2:     /* 面板亮部：#5a4228 附近 */
  --border:      /* 黃銅邊框：#8a6a3f，重要框可亮到 #b08a4f */
  --border-soft: /* 次要邊框 */
  --text:        /* chrome 上的字：象牙白 #f4e8cd */
  --dim:         /* chrome 上的次要字 */

  /* ── 羊皮紙（內容列/卡片：淺底深字） ── */
  --paper:       /* 紙面亮部：建議提亮到 #f2e4c2（現在太土黃） */
  --paper-2:     /* 紙面中間調 */
  --paper-deep:  /* 紙面暗部/邊緣 */
  --ink:         /* 紙上的墨字：#4a3018 */
  --ink-dim:     /* 紙上次要字 */
  --paper-line:  /* 紙的描邊 */

  /* ── 強調色（稀缺使用） ── */
  --gold:        /* 主行動金：#f2c95c */
  --gold-deep:
  --blue:        /* 寶石藍 CTA（前往/創建類）：#7fc4e8 */
  --hp:          /* 成功/生命綠 */
  --danger:      /* 警示紅 */

  /* ── 元素色（卡面/戰鬥共用，勿改值——pixi 端寫死同色） ── */
  --fire --wind --water --light --dark
}
```

**新增權**：你可以新增變數（例如 `--wood-texture`、陰影 token），命名用 `--ui-*` 前綴避免衝突。

---

## 4. 元件庫 API（依重要度排序）

每個元件列出：class、DOM 結構、狀態、期望視覺。

### 4.1 羊皮紙卡（`.paper`／`.gd-paper`）
- **用在**：所有內容列（公會列、成員列、商店卡、留言板…），是整個風格的主角。
- **DOM**：單一 `div`，內容自由。
- **期望**：象牙紙面（提亮！）＋上下撕邊或毛邊質感（可用 border-image 9-slice 或 mask）；頂部 1px 高光、底部壓深；hover 微浮起。目前純漸層版太平。
- 內文字色一律 `--ink`／`--ink-dim`（JS 已在元件內指定 class，紙上不會出現淺色字）。

### 4.2 六角綢帶鈕（`.gd-hexbtn`，修飾詞 `.stone` `.blue` `.mini` `.wide` `.done`）
- **用在**：公會頁所有動作。**希望推廣成全遊戲的標準按鈕**（可加新 class 別名如 `.ui-ribbon`，但舊 `.btn`/`.btn-gold` 也要一起換皮，兩者並存）。
- **期望**：金屬綢帶質感（上亮下暗、邊緣高光、按下內凹）；`clip-path` 六角切角維持；`.stone`＝石灰銀（次要）、`.blue`＝寶石藍（前往/創建）、`.done`＝去飽和壓灰。

### 4.3 木刻橫幅標題（`.gd-banner`、`.page-title`）
- **期望**：`.gd-banner` 做成雕花木牌（可素材化）：深木底、描金字、兩端裝飾切角。`.page-title`（每頁左上標題字）維持金字＋底部金飾條，但字體可加襯線感（見 §7 字體）。

### 4.4 盾徽框（`.gd-shield`，`.big` 變體）
- **用在**：公會徽章。**期望素材化**：金屬包邊的盾形底座，中央放紋章圖（目前是 emoji 佔位，素材清單含 6~8 個紋章 SVG）。

### 4.5 通用按鈕（`button`、`.btn`、`.btn-gold`、`.btn-danger`、`.icon-btn`）
- 全遊戲既有按鈕。換皮方向同 4.2（黃銅邊、皮革底／金綢帶）。

### 4.6 藥丸與徽章（`.pill`、`.dot`、`.hs-tag` 系列）
- `.pill`：頂欄貨幣（金幣/券/鑽）。期望：黃銅包邊小膠囊、圖示凹槽感。
- `.hs-tag`（角色詳情的職業/屬性/種族/系列標籤）：`.cls`（金框）`.el`（元素色框）`.race`（綠框）`.series`（金字）——保持語義色，質感升級。
- `.dot`：紅點通知，保留。

### 4.7 面板／彈窗（`.hs-panel`、`.ov-*` 系列、`.hx-rail`）
- `.hs-panel`：角色詳情右欄。期望：深皮革底＋黃銅描邊＋四角鉚釘。
- 彈窗（`.ov-playercard`、`.ov-arena-board` 等，共用 `.ov-title`、`.ov-close`）：同語言；`.ov-close` 做成銅釦叉。
- `.hx-rail`：英雄頁左側篩選欄——深色半透明改成皮革條。

### 4.8 卡面（`.cardframe` 系列——謹慎處理）
- `.cardframe-inner`（內金線）、`.cardframe-elbadge`（左上屬性寶石）、`.cardframe-clsbadge`（右下職業章）、`.cardframe-lvl`、`.cardframe-name`、`.cardframe-stars`。
- 立繪是程式生成 SVG（會換成 AI 圖），**卡框可素材化**：木質雕花外框＋金屬內線（9-slice）。徽章底座可做成寶石鑲嵌。

### 4.9 首頁大廳（`.hub-*` 系列）
- `.hub-tl`（玩家名牌）、`.hub-cur`（貨幣列）、`.hub-feat/.fsq`（右上功能格）、`.hub-adv`（右下出征大鈕）、`.hub-dock/.hub-dk/.dksq/.dkl`（底部功能碼頭）。
- 期望：功能格與碼頭做成**木質吊牌／銅飾圖示格**；出征鈕是全頁最強視覺（金綢帶大鈕＋微呼吸光）。
- 背景（`#screen-home` 黃昏山谷漸層＋`.hub-hills` 山影）可整體重繪成插畫背景圖（素材）。

### 4.10 戰鬥 HUD（DOM overlay：`.bo-*` 系列）
- `.bo-cb`（倍速/快轉圓鈕）、`.bo-env`（環境徽章）、頂部隊伍血量匯總、`.hub-*` 不含。canvas 內部不動。
- 期望：深色半透明底＋黃銅邊，維持高對比可讀性（戰鬥上方不能搶戲）。

### 4.11 其他頁面既有 class 前綴速查
| 前綴 | 頁面 | 備註 |
|---|---|---|
| `.hx-*` | 英雄頁（篩選欄/頁簽/計數/卡格 `.deck/.deck-item`） | `.deck-item` 有 `content-visibility` 效能屬性，勿移除 |
| `.hs-*` | 角色詳情（單頁面板：標籤/等級卡/數值列/技能列/CTA） | |
| `.tcard`/`.tp-*`/`.swap-*` | 隊伍頁與替換抽屜 | |
| `.gx-*` | 召喚頁（卡池軌/看板/召喚鈕）＋ `.summon-*` 揭曉演出 | |
| `.sh-*` | 商店 | 商品卡已 paper 化 |
| `.ar-*`/`.arb-*`/`.arr-*` | 競技場（對手列/防守隊/排行/戰報） | 對手列建議 paper 化 |
| `.fr-*` | 好友 | 列表建議 paper 化 |
| `.gd-*` | 公會 | 新風格旗艦，已按目標做了一版，可再加質感 |
| `.tw-*` | 試煉塔 | 塔軌里程碑 |
| `.pc-*` | 玩家名片彈窗/輸入框 | `.pc-input.paper-input` 為紙上輸入框變體 |
| `.qs-*`/簽到/掛機箱 | 任務/簽到/掛機（`metaSheets`） | 彈窗內容列建議 paper 化 |
| `.ftue-*` | 新手引導氣泡 | |
| `.toast` | 提示 | 建議做成小羊皮紙條 |

---

## 5. 頁面內容清單（每頁顯示什麼、有什麼按鈕）

> 這是每個畫面的完整內容盤點——工程師不需要猜任何頁面該放什麼。
> 「內容」＝要顯示的資訊；「互動」＝可點擊的元素與行為。所有互動已由 JS 綁好，換皮不影響。

### 5.0 全頁共通
- 左上**返回鈕**（`.back-btn`，回主城）＋左上**頁面標題**（`.page-title.left`）
- 各頁背景為該頁專屬漸層/場景（`#screen-*`）

### 5.1 首頁大廳（`#screen-home`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 左上名牌 `.hub-tl` | 頭像、暱稱、主線進度（章-節） | 點→玩家名片彈窗（改暱稱/頭像/簽名） |
| 右上貨幣列 `.hub-cur` | 金幣/召喚券/精華 三顆 `.pill` ＋ 設定鈕 | 設定→設定彈窗 |
| 右上功能格 `.hub-feats` | 好友、任務、簽到、掛機箱（四格 `.hub-feat`，帶紅點） | 各自開頁/彈窗 |
| 中央 | 看板英雄立繪 `.hub-hero`＋對話泡泡 `.hub-say`（隨機台詞） | 點英雄→換一句台詞 |
| 右下 `.hub-adv` | 出征大鈕：關卡號＋「立刻出發！」 | 進戰役頁開打 |
| 底部碼頭 `.hub-dock` | 七個功能格 `.hub-dk`：召喚/商店/隊伍/英雄/公會/競技場/試煉塔 | 進各頁 |

### 5.2 隊伍頁（`#screen-team`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 頂部中央 | 「5/6 上陣」計數膠囊 | — |
| 主區 | 六個直式全身卡槽，左群「後衛」右群「前衛」（`.tp-*`/`.tcard`）。**排列合約：由右到左＝站位 1→6**（最右＝前排 1 號位最先出手），左→右渲染順序 [6,5,4]｜[3,2,1]，金線分隔兩群 | 點卡→角色詳情；**拖曳卡片**互換站位；點空格→開替換抽屜 |
| 卡面 | 屬性色外框、右上屬性寶石、Lv 章、職業章、底部名牌、出戰中無緞帶（本頁全是出戰卡） | — |
| 底部 | 操作提示文字（拖曳調站位/點卡看詳情） | — |
| 右下 | 「英雄替換」鈕 | 開底部抽屜：持有英雄橫向捲動列，點卡入陣/替換（`.swap-*`） |

### 5.3 英雄頁（`#screen-heroes`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 右上頁簽 `.hx-tabs` | 「英雄」（持有卡冊）/「圖鑑」（全卡收集） | 切換 |
| 左篩選欄 `.hx-rail` | 三段 chips：職業（坦/打/輔，帶職業章）、屬性（五色）、種族（八個）；有選取時出現「✕清除篩選(n)」 | 多選過濾（即時顯隱，不重載） |
| 右上計數 `.hx-count` | 英雄頁「顯示 x / 擁有 y」；圖鑑「收集進度 x / 總數」 | — |
| 卡格 `.deck` | 卡面（同 5.2）＋「出戰中」綠緞帶；圖鑑未擁有＝灰剪影＋🔒 | 點/長按→角色詳情；圖鑑未擁有→提示並跳召喚 |

### 5.4 角色詳情（`.hero-sheet` 全螢幕 overlay）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 左半 | 滿版立繪（呼吸動畫）、左下名牌（屬性寶石+名字+星級）、底部圓點頁標、左右 ‹ › | 左右鍵/滑動/箭頭切換英雄；Esc/返回鈕關閉 |
| 右面板 `.hs-panel` 由上而下 | ① 標籤列：職業章+職業、屬性寶石+屬性、種族、系列（可多個）② 等級卡：Lv現值 » 下一級＋星級 ③ 數值三列：生命/攻擊/防禦＋下一級藍字增量 ④ 星級說明＋里程碑膠囊（達成點亮）⑤ 技能區：絕技（名+自動描述）、進場🌀/光環✨/觸發⚡/隊伍技👥 各一列 ⑥ 滿版金色「強化」鈕（顯示精華/金幣費用；可按住連續升級；滿級顯示 MAX） | 強化鈕；其餘唯讀 |

### 5.5 召喚頁（`#screen-gacha`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 右上 | 召喚券餘額 | — |
| 左卡池軌 `.gx-rail` | 卡池卡片（「命運召喚·常駐」＋「新卡池·敬請期待」佔位） | 切換卡池 |
| 中央 | 看板角色大立繪輪播＋屬性標籤+名字+職業·系列標籤 | — |
| 右側 | 池名、說明文（機率方向、重複轉精華）、「機率公示」鈕 | 機率彈窗（各稀有度機率表） |
| 右下 | 「1 次召喚」「10 次召喚」鈕（顯示券費用） | 召喚→揭曉演出（翻卡、可跳過）→結算列表＋「確定」 |

### 5.6 商店（`#screen-shop`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 每日特惠區 | 標題「每日特惠（每日 5:00 更新）」；免費禮卡＋特惠卡×3：圖示、名稱、內容量、原價劃線、現價、OFF% 斜標、今日限購 x/y | 點卡購買（扣金幣、獎勵飛行動畫、售罄置灰） |
| 兌換所區 | 常駐商品×3：同卡面、無折扣標 | 同上 |

### 5.7 競技場（`#screen-arena`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 右上 | 段位徽章+段位名+分數、今日挑戰 x/5、連線狀態 | — |
| 左對手列 `.ar-foe`×3 | 頭像、暱稱、分數、防守隊 mini 卡×5、「挑戰」鈕 | 挑戰→跳戰役頁播放戰報→勝敗與分數增減結算彈窗 |
| 左底 | 「刷新對手（1,000 金幣）」 | 換一批對手 |
| 右欄 | 「我的防守隊」3×2 mini 卡、「用目前隊伍當防守隊」鈕、「排行榜」「戰報」鈕 | 排行榜/戰報彈窗（列表） |

### 5.8 好友（`#screen-friends`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 頂部 | 搜尋框（輸入暱稱/ID）＋「加好友」；右側友情點數 | 送出好友申請 |
| 禮物列 | 待領禮物膠囊 | 一鍵領取 |
| 好友列 `.fr-row` | 頭像、暱稱、上線狀態、「送禮」（已送置灰）、「切磋」、「刪除」 | 切磋→播放友誼賽戰報（不扣次數）；點頭像→名片 |

### 5.9 公會（`#screen-guild`）——已為新風格樣板
**未入會**：木刻橫幅「推薦公會」、欄位標題列（公會名/等級/人數）、羊皮紙公會列（盾徽、名、公告摘要、Lv、人數、「加入/申請」鈕）、底部「創建公會（50,000 金幣）」→建會彈窗（名稱輸入＋加入方式切換）。
**入會（三欄）**：左＝盾徽資訊卡（名/Lv/人數/我的公會幣）、羊皮紙公告（幹部可✏️編輯）、簽到/捐獻/公會商店/入會申請(n)/退出公會 綢帶鈕直排；中＝「公會人數 x/30」＋成員卷軸列（木框頭像、暱稱、綠字職位[會長][副會長]、本週活躍；會長看得到升職/降職/踢出小鈕；點列→名片）；右＝Boss 戰報卡（名/Lv/血條/剩餘血量、「⚔挑戰（每日2次）」「傷害排行」）＋羊皮紙留言板（20 則、輸入框＋送出）。
**彈窗**：捐獻（三檔位表）、公會商店（品項/幣價/週限/等級鎖）、入會申請（同意/拒絕）、傷害排行（名次表）。

### 5.10 試煉塔（`#screen-tower`）
| 區塊 | 內容 | 互動 |
|---|---|---|
| 左塔軌 | 樓層列表（已通關✓/當前層高亮/未達灰）、每 5 層里程碑獎勵標記 | 捲動 |
| 右欄 | 當前層資訊（層數、主題屬性、敵人等級、環境）、里程碑說明卡、「挑戰」鈕 | 挑戰→戰役頁播放→勝利彈窗（獎勵、下一層） |

### 5.11 戰鬥 HUD（DOM overlay，canvas 之上）
| 位置 | 內容 | 互動 |
|---|---|---|
| 左上 | 返回鈕＋「我方」隊伍血量匯總條 | 返回＝中止觀戰 |
| 中上 | 關卡菱標（1-1）＋環境徽章（天氣·場地） | — |
| 右上 | 敵隊名＋血量匯總條＋敵徽 | — |
| 左下 | 回合數 R# | — |
| 右下 | 倍速鈕（×1/×2）＋快轉鈕⏭ | 切倍速/瞬間結算 |
| 底部中央 | 「我方 x vs 敵方 y」存活計數 | — |
| 事件層 | 勝利/戰敗/平手橫幅（勝：金幣+下一關；敗：「調整陣容」CTA）、絕技聚光燈技能名標籤 | — |

### 5.12 彈窗全家（`#overlay-root`）
- **任務**：每日任務列（描述、進度條、領取鈕/已領）
- **簽到**：七日格（今日高亮、已領勾）、「簽到領取（獎勵）」大鈕
- **掛機箱**：累積時間、金幣/精華預覽、「領取」
- **玩家名片**：頭像（可選）、暱稱/簽名編輯、戰力、防守隊 3×2
- **確認 sheet**（`confirmSheet`）：標題/描述/取消/確認（危險操作紅鈕）
- **Toast**：短提示（帶 icon），建議羊皮紙短條樣式

---

## 5-bis. 頁面優先序與驗收畫面

改版順序建議（每頁完成後截 1280×720 對比圖驗收）：

1. **首頁大廳**（第一印象）
2. **公會頁**（已有一版新風格，補質感到位＝全遊戲樣板）
3. **英雄頁＋角色詳情**（使用時間最長）
4. **隊伍頁**
5. 召喚、商店、競技場、好友、試煉塔
6. 彈窗全家（任務/簽到/掛機/名片/確認 sheet/toast）

**驗收標準**：
- 三層對比成立：暗木底 ↔ 亮紙內容 ↔ 金/寶石點綴（截圖轉灰階仍能分清層次）
- 紙上深字對比 ≥ WCAG AA（4.5:1）；chrome 上象牙字同標準
- 無版面位移（layout 不變，只換皮）
- 60fps：hover/按壓動效只用 transform/opacity/filter
- `deck-item` 的 `content-visibility: auto` 與各捲動容器行為保留

---

## 6. 素材需求清單（`public/assets/ui/`，SVG 或 2x PNG）

| 素材 | 規格 | 用途 |
|---|---|---|
| 羊皮紙 9-slice | 撕邊、四角毛邊，中心可平鋪 | `.paper` 全系 |
| 木紋底圖 | 可平鋪暗胡桃木 | `--bg` 頁面底 |
| 木刻橫幅 | 兩端雕花，9-slice 橫向拉伸 | `.gd-banner`/區塊標題 |
| 金綢帶鈕 9-slice | 常態/按下 兩態 | `.gd-hexbtn`/`.btn-gold` |
| 石綢帶鈕 9-slice | 同上 | `.gd-hexbtn.stone` |
| 藍綢帶鈕 9-slice | 同上 | `.blue` CTA |
| 黃銅面板框 9-slice | 四角鉚釘 | `.hs-panel`/彈窗 |
| 盾徽底座 | 金屬包邊盾形 | `.gd-shield` |
| 紋章圖 ×8 | 龍/獅/鷹/狼/百合/劍/錨/星 | 公會徽章 |
| 木吊牌 | 底部碼頭功能格 | `.hub-dk` |
| 銅釦關閉鈕 | | `.ov-close` |

沒有素材前，一律先用 CSS 近似（現況即是），素材到位逐步替換。

---

## 7. 字體與動效

- **字體**：中文維持系統字（PingFang TC/微軟正黑），但**標題層**（`.page-title`、`.gd-banner`、`.ov-title`、卡名）加 `letter-spacing` 與更重的字重對比；數字一律 `font-variant-numeric: tabular-nums`（多處已設，保持）。若引入自訂標題字型（如思源宋體 subset），需打包進 repo，不可外連 CDN。
- **動效節奏**：進場 stagger 已由 JS 控制（`staggerIn`），CSS 只補 hover（浮起 2~4px＋陰影）與按壓（已有 `.pressable`）。金色主鈕可加 3~4s 週期的微光掃過（`@keyframes` + mask，低調）。
- **禁止**：全頁面級的持續動畫（耗電）、layout 抖動、`transition: all`。

---

## 8. 已知地雷（改壞會炸的東西）

1. `.deck-item` 的 `content-visibility: auto` + `contain-intrinsic-size`——英雄頁效能命脈，只能改視覺屬性。
2. `.screen`/`.screen.active` 的 display 切換、`#overlay-root` 層——router 依賴。
3. `.pressing::after` 按壓高亮由手勢系統動態掛，保留 selector。
4. 元素色變數（`--fire` 等）與 PixiJS 端寫死同色值，改了會跟戰鬥棋子對不上。
5. `.hs-stats .v small.up`（升級增量藍字）、`.st-mile.on`（星級里程碑點亮）這類「狀態語義色」要保留可辨識性。
6. 三個 CSS 檔有少量 `!important`（`.paper-input`），動之前先全域搜尋。

---

## 9. 交付物

- 三個 CSS 檔的視覺層改版（diff 乾淨、變數優先）
- `public/assets/ui/` 素材與引用
- 每頁 1280×720 前後對比截圖
- 若新增 token/素材：更新本文件 §3/§6 清單
